# Quix Project Descriptor
# This file describes the data pipeline and configuration of resources of a Quix Project.

metadata:
  version: 1.0

# This section describes the Deployments of the data pipeline
deployments:
  - name: RunLLM MCP Server
    application: runllm-mcp
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 500
      replicas: 1
    publicAccess:
      enabled: true
      urlPrefix: runllmmcp-quixguru
    variables:
      - name: RUNLLM_API_KEY
        inputType: Secret
        required: true
        secretKey: RUNLLM_API_KEY
      - name: RUNLLM_PIPELINE_ID
        inputType: FreeText
        required: true
        value: 111
  - name: MCP Quix Streaming Reader
    application: mcp-quix-streaming-reader
    version: latest
    deploymentType: Service
    resources:
      cpu: 200
      memory: 500
      replicas: 1
    publicAccess:
      enabled: true
      urlPrefix: mcpquixstreamingreader-6a1
    variables:
      - name: QUIX_PAT_TOKEN
        inputType: Secret
        required: true
        secretKey: API_PAT

